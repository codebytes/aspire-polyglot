<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <!--
      OpenTelemetry Logback appender: bridges SLF4J/Logback log records to the
      OpenTelemetry SDK LoggerProvider configured by opentelemetry-spring-boot-starter.
      Each record is stamped with the active trace_id / span_id and exported via OTLP.
      Without this declaration the starter has no appender to install the SDK into,
      so logs would never flow to the Aspire dashboard.
    -->
    <appender name="OpenTelemetry"
              class="io.opentelemetry.instrumentation.logback.appender.v1_0.OpenTelemetryAppender">
        <captureExperimentalAttributes>true</captureExperimentalAttributes>
        <captureKeyValuePairAttributes>true</captureKeyValuePairAttributes>
        <captureMdcAttributes>*</captureMdcAttributes>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="OpenTelemetry"/>
    </root>
</configuration>
